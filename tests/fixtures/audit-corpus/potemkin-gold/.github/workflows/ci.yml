name: gate
on:
  push:
  pull_request:
jobs:
  grade:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: product-on-purpose/agent-skills-toolkit@v1.18.0
        with:
          path: .
